# Database configuration (uses SQLite by default)
DATABASE_URL=sqlite:///career_compass.db

# Frontend URLs allowed to access the backend API (comma-separated list)
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
